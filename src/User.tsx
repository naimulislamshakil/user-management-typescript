import React, { FC } from "react";

interface Users {
  id?: number;
  name: string;
  userName: string;
  email: string;
}
const User: FC<Users> = ({ user }) => {
  return <div>kjbdjcj</div>;
};

export default User;
